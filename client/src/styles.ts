import styled from 'styled-components';

export const AppWrapper = styled.div`
  max-width: 28rem;
  margin: 0 auto;
  height: 100vh;
  display: flex;
  flex-direction: column;
  background: linear-gradient(135deg, #FFF5EC, #FFE8D4, #FFD9B8);
`;

export const MainContent = styled.div`
  flex: 1;
  overflow: auto;
  padding: 1rem;
`;